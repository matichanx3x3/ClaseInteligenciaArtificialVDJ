using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CAR_IA : MonoBehaviour
{
   
    [SerializeField] CarPhysics controlledCar;

    // Update is called once per frame
    void FixedUpdate()
    {



        /*
        
        controlledCar.SetThrust(1);
        controlledCar.SetSteering(0);
        controlledCar.SetBrake(1);

         */
    }
}
